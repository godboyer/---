// import React from 'react';
// import { Modal, message } from "antd";


// let loader:any  = null 
// export const ShowLoading= (msg = '加载中') => {
//   loader = message.loading(msg, 0, () => {
         
//   });
  
// }

// export  const ShowSuccsess = (msg = '请求数据成功') => {
//         loader && loader.close()
//         message.destroy()
//         message.success(msg)

// }
   
// export const ShowError = (msg = '请求失败') => {
//       loader && loader.close()
//       message.destroy()
//       message.error(msg)

// }
   
export const ShowMsg= (msg = '请阅读并勾选协议') => {
      // loader && loader.close()
      // message.destroy()
      // message.info(msg)
}


